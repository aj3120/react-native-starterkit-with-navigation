import React from 'react';
import { View, Text } from 'react-native';
import { styles } from './styles';

export default function ShotsScreen() {
    return(
        <View style={styles.container}>
            <Text>ShotsScreen</Text>
        </View>
    );
}
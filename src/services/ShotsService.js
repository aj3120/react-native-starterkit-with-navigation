import { shots } from './shots';
// Read state - shots pagination data and filter data
// Api call to fetch the shots - take some filters and pagination data
// Like a shot
// Api call to fetch the releases
// Inital data fetch from the server
// Conferences

export const fetchShots = () => {
    return {
        shots
    }
}